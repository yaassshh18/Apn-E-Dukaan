# Django Production Settings
SECRET_KEY=your_super_secret_key_here
DEBUG=True

# Database Credentials (if applicable, uncomment and configure for postgres/mysql)
# DATABASE_URL=postgres://user:password@hostname:port/db_name

# Email Notifications Settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Sensitive Email Credentials (Must be set!)
EMAIL_HOST_USER=apn.e.dukaan.main@gmail.com
EMAIL_HOST_PASSWORD=your_secure_app_password
